#include <stdio.h>
int main(){
  
  return 0;
}if(arr[i] == arr[j])
      {
        if(arr[i][j] != 1)
        {
          printf("NO");
          is_no_printed = true;
          break;
        }
      }