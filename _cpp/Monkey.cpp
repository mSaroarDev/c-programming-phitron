#include <bits/stdc++.h>
using namespace std;
int main()
{
  string str;
  while (getline(cin, str))
  {
    string word = str;
    sort(word.begin(), word.end());

    // cout << word << endl;
    while(word)
    {
      cout << c;
    }
  }
  cin.ignore();

  return 0;
}